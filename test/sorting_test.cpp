// Catch2 provides its own main function so we just have to run these macros to setup some data that the main function knows how to read.
#include "catch2/catch_test_macros.hpp"

TEST_CASE("Merge Sort", "[sorting]")
{
   SECTION("empty array")
   {
   }
}
