#include "livingquality.h"
#include <algorithm>
#include <ranges>

uint32_t LivingQuality::MinimumMedianRectangle(const Core::Grid<uint32_t>& CityQualityRankings, uint32_t RectWidth,
                                               uint32_t RectHeight)
{
   for(int i = 0; i < CityQualityRankings.GetWidth())
}
