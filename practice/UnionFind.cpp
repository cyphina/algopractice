#include <utility>
#include <vector>

// You are given n nodes and edges.
// Each union merges two nodes into a group.
// Return how many groups have size >= K.
// Each pair represents a union operation.

int countGroupsOfSizeAtLeastK(int n, const std::vector<std::pair<int, int>>& edges, int K)
{
}
